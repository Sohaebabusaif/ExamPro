# 📖 دليل تشغيل نظام ExamPro Elite

مرحباً بك في نظام **ExamPro Elite**! هذا الدليل سيساعدك على تشغيل النظام على أي جهاز.

## 🚀 كيفية التشغيل

بما أن النظام مصمم ليعمل كتطبيق ويب متقدم (PWA) وبدون إنترنت، اتبع الخطوات التالية للتشغيل المثالي:

### الطريقة الأولى: التشغيل كخادم محلي (موصى بها)
للحصول على كامل الميزات (بما فيها تثبيت التطبيق على الجهاز والكاميرا):
1. قم بفتح مجلد المشروع باستخدام برنامج **VS Code**.
2. قم بتثبيت إضافة **Live Server**.
3. انقر بزر الفأرة الأيمن على ملف `index.html` واختر **Open with Live Server**.
4. سيفتح النظام في المتصفح، وسيظهر لك خيار لتثبيته كبرنامج مستقل (Install).

### الطريقة الثانية: التشغيل المباشر (للأجهزة البسيطة)
1. في أجهزة الويندوز: انقر مرتين على ملف `تشغيل_للكمبيوتر_ويندوز.bat`.
2. في أجهزة الماك/لينكس: قم بتشغيل الملف `تشغيل_للماك_ولينكس.sh`.
*(ملاحظة: بعض ميزات المراقبة قد تتطلب تشغيل النظام عبر خادم محلي كما في الطريقة الأولى لأسباب أمنية في المتصفحات).*

---

## 🔒 لوحة الإدارة وكلمة المرور
للدخول إلى لوحة الإعدادات (أيقونة الترس ⚙️ في الشاشة الرئيسية):
- النظام يطلب كلمة مرور للدخول للإدارة.
- تم تشفير كلمة المرور في ملف `js/app.js` (ابحث عن المتغير `passwordHash`).
- *ملاحظة:* إذا أردت تغيير كلمة المرور، ستحتاج إلى تحويل كلمتك الجديدة إلى نظام التشفير المستعمل في التطبيق، أو التواصل مع الدعم الفني لتحديثها.

## 📁 محتويات المجلد
- `index.html`: واجهة النظام الرئيسية.
- `css/`: مجلد التصميم والألوان.
- `js/`: مجلد البرمجة (يحتوي على `app.js` وهو القلب النابض للنظام).
- `sw.js` & `manifest.json`: ملفات المسؤولة عن تحويل النظام لتطبيق يعمل بدون إنترنت (PWA).

مع تمنياتنا لك بتجربة امتحانات موفقة واحترافية!
