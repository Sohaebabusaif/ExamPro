# دليل استخدام نظام الامتحانات الإلكتروني أونلاين (Online ExamPro)

تم تطوير هذا النظام ليكون متصلاً بالإنترنت (أونلاين) باستخدام خادم (Node.js/Express) وقاعدة بيانات (SQLite) لحفظ بيانات الامتحانات والطلاب مركزياً.

## متطلبات التشغيل
- تثبيت [Node.js](https://nodejs.org/) على جهازك أو الخادم (Server).
- وجود الملفات المرفقة في مجلد المشروع الخاص بك.

## كيفية تشغيل النظام لأول مرة
1. افتح موجه الأوامر (Command Prompt أو Terminal) وتوجه إلى مسار المجلد.
2. قم بتثبيت الاعتماديات المطلوبة بكتابة الأمر التالي:
   ```bash
   npm install express cors body-parser sqlite3
   ```
3. ابدأ تشغيل الخادم بالأمر التالي:
   ```bash
   node server.js
   ```
4. سيظهر لك في الشاشة `Server is running on http://localhost:3000`.

## دورة عمل النظام (كيفية استخدامه)

### 1. إعداد الامتحان (للمعلم)
- افتح المتصفح على الرابط: `http://localhost:3000`
- ادخل إلى **لوحة الإدارة** بإدخال كلمة المرور.
- أضف الأسئلة وقم بضبط إعدادات الامتحان (الوقت، التوزيع العشوائي، المراقبة...).
- عند الضغط على زر "حفظ"، سيقوم النظام برفع الأسئلة وقواعد الامتحان إلى قاعدة البيانات.
- **تنبيه:** سيظهر لك إشعار به الرابط المخصص للامتحان (مثال: `http://localhost:3000/?examId=exam_123xyz`). قم بنسخ هذا الرابط ومشاركته مع الطلاب!

### 2. تقديم الامتحان (للطالب)
- يقوم الطالب بالضغط على الرابط الذي شاركه المعلم `http://localhost:3000/?examId=...`
- سيقوم النظام مباشرةً بسحب الأسئلة والتوقيت من الخادم وعرض واجهة إدخال بيانات الطالب (الاسم والصف).
- بمجرد الضغط على "دخول"، سيبدأ الامتحان وتبدأ جميع ميزات المراقبة المتصلة.
- عند قيام الطالب بـ "تسليم" الامتحان أو انتهاء الوقت، تظهر له الشهادة، ويتم إرسال نتيجته وإجاباته ومخالفاته إلى الخادم لتُسجل في قاعدة البيانات.

### 3. استعراض النتائج (للمعلم)
- لترى درجات الطلاب الفورية، يمكنك الدخول مجدداً إلى النظام وفتح موجه أوامر المتصفح (`Console` عبر `F12`) واستدعاء الدالة التالية:
  ```javascript
  fetchStudentResults('exam_123xyz');
  ```
- قريباً: سيتم ربط هذه الدالة بشكل مباشر في صفحة "سجل النتائج" بلوحة المعلم بضغطة زر.

## الملفات الجديدة التي أضيفت للمشروع
* `server.js`: الملف الذي يحتوي على الخادم وقاعدة البيانات (`sqlite3`) للتعامل مع الـ (APIs).
* `js/online_logic.js`: ملف واجهة المستخدم الذي يستبدل التخزين المحلي ويرسل البيانات للخادم، وتم ربطه بملف `index.html`.

نتمنى لك تجربة امتحانات مميزة وفعالة!
